# API do PABX

A construir. É a peça que elimina a lista de "o que ainda precisa ser feito à
mão" do README da raiz.

**Precisa ser PHP rodando dentro do FusionPBX**, usando as classes e a camada de
banco dele. Não é opinião de estilo: escrever no Postgres direto produz
configuração que existe no banco, aparece na tela e é invisível para o
FreeSWITCH — ver `../docs/armadilhas.md`, item 1.
